const marks = ["X", "O", "X", "O", "X", "O", "X", "O", "X"];

function Render(){
    for(let i = 0; i < marks.length; i++){
        let element = marks[i];
        console.log(element);
    }
}

Render();